# Component catalog

Every component the library ships. Each is a directory under `src/components/`; its `_component` path is that directory path in kebab-case. This is the canonical list of what exists and when to use each — other skills link here.

**Full prop list for any entry:** open its `.astro` file and read the `Astro.props` destructure. The file path is mechanical — the last path segment PascalCased: `page-sections/heroes/hero-center` → `src/components/page-sections/heroes/hero-center/HeroCenter.astro`. For page sections, the editor-facing defaults and input types also live in the sibling `<slug>.cloudcannon.structure-value.yml` + `.inputs.yml`. The tables below list the **content** props an author sets — not every prop.

## Page sections

Place these directly in a page's `pageSections` array. **All page sections also take the standard shell props** (`sectionLabel`, `maxContentWidth`, `paddingHorizontal`, `paddingVertical`, `colorScheme`, `backgroundColor`, `background`) — see [SKILL.md → Standard section props](SKILL.md#standard-section-props-every-page-section). Only content props are listed here.

<!-- generated:catalog:page-sections:start (npm run docs:catalog) -->

### Heroes

| `_component`                       | Use for                                                      | Key content props                                                                                                                                                              |
| ---------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `page-sections/heroes/hero-center` | Centered hero with a heading, supporting text, and buttons.  | `eyebrow`, `heading`, `subtext` (markdown), `buttonSections[]`                                                                                                                 |
| `page-sections/heroes/hero-split`  | Hero with text on one side and an image on the other.        | `eyebrow`, `heading`, `subtext` (markdown), `imageSource`, `imageAlt`, `imageAspectRatio` (`none`/`square`/`landscape`/`portrait`/`widescreen`), `buttonSections[]`, `reverse` |
| `page-sections/heroes/page-header` | Slim title block for inner pages, with optional breadcrumbs. | `eyebrow`, `heading`, `subtext` (markdown), `showBreadcrumbs`, `alignmentHorizontal` (`start`/`center`)                                                                        |

### Builders

| `_component`                            | Use for                                      | Key content props                                                                                                                                                                                                                                                                                                                                                                                                             |
| --------------------------------------- | -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `page-sections/builders/custom-section` | Groups and styles a large section of a page. | `label`, `contentSections[]`, `maxContentWidth` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`), `paddingHorizontal` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`/`4xl`/`5xl`/`6xl`), `paddingVertical` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`/`4xl`/`5xl`/`6xl`), `colorScheme` (`inherit`/`light`/`dark`), `lockColorScheme`, `backgroundColor` (`none`/`base`/`surface`/`accent`/`highlight`), `background`, `rounded` |

- **`custom-section`**: The escape hatch for arbitrary layouts; `rounded` is unique to this section (page-section wrappers do not forward it).

### Collections

| `_component`                                | Use for                                                                  | Key content props                                                                                                                                                                                                       |
| ------------------------------------------- | ------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `page-sections/collections/card-collection` | Grid of linked cards for services, case studies, projects, or resources. | `eyebrow`, `heading`, `subtext` (markdown), `items[]`, `layout` (`grid`/`masonry`), `columns`, `gap` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`), `aspectRatio` (`landscape`/`widescreen`/`square`/`portrait`/`none`) |
| `page-sections/collections/gallery-grid`    | Image grid with captions. Click a photo to view it full size.            | `eyebrow`, `heading`, `subtext` (markdown), `images[]`, `layout` (`grid`/`masonry`), `columns`, `gap` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`), `aspectRatio` (`square`/`landscape`), `lightbox`, `showThumbnails` |
| `page-sections/collections/latest-posts`    | Newest blog posts as cards, loaded automatically when the site builds.   | `eyebrow`, `heading`, `subtext` (markdown), `count`, `tag`, `showViewAllButton`, `viewAllLabel`                                                                                                                         |
| `page-sections/collections/team-grid`       | Team member cards in a three-column grid.                                | `eyebrow`, `heading`, `subtext`, `teamMembers[]`                                                                                                                                                                        |

- `card-collection` item (`items[]`): `image`, `imageAlt`, `link`, `contentSections`.
- `gallery-grid` item (`images[]`): `image`, `alt`, `caption`.
- `team-grid` item (`teamMembers[]`): `name`, `role`, `bio`, `imageSource`, `imageAlt`.

### Conversion

| `_component`                                  | Use for                                                                                            | Key content props                                                                                                                                                |
| --------------------------------------------- | -------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `page-sections/conversion/contact-split`      | Contact details beside a form, with an optional map.                                               | `eyebrow`, `heading`, `subtext` (markdown), `details[]`, `formAction`, `formBlocks[]`, `mapEmbedUrl`                                                             |
| `page-sections/conversion/cta-center`         | Centered call to action with a headline, supporting text, and buttons.                             | `heading`, `subtext` (markdown), `buttonSections[]`                                                                                                              |
| `page-sections/conversion/cta-form`           | Call to action with a form on one side and an image on the other.                                  | `heading`, `subtext` (markdown), `formAction`, `formBlocks[]`, `imageSource`, `imageAlt`, `reverse`                                                              |
| `page-sections/conversion/cta-split`          | Call to action with text on one side and buttons on the other.                                     | `heading`, `subtext` (markdown), `imageSource`, `imageAlt`, `buttonSections[]`, `reverse`                                                                        |
| `page-sections/conversion/pricing-comparison` | Table that compares features across plans.                                                         | `eyebrow`, `heading`, `subtext` (markdown), `plans[]`, `rows[]`, `striped`                                                                                       |
| `page-sections/conversion/pricing-tiers`      | Plan cards with feature lists, a highlighted plan, and an optional monthly or annual price switch. | `eyebrow`, `heading`, `subtext` (markdown), `showAnnualPricing`, `monthlyLabel`, `annualLabel`, `annualBadge`, `defaultInterval` (`monthly`/`annual`), `tiers[]` |

- `pricing-tiers` item (`tiers[]`): `name`, `price`, `priceSuffix`, `annualPrice`, `annualPriceSuffix`, `description`, `features`, `buttonText`, `buttonLink`, `highlight`, `badgeText`.

### Explainers

| `_component`                              | Use for                                                                  | Key content props                                                                                                                                                                              |
| ----------------------------------------- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `page-sections/explainers/faq-section`    | Frequently asked questions in an accordion.                              | `heading`, `items[]`                                                                                                                                                                           |
| `page-sections/explainers/feature-grid`   | Grid of features with icons and short descriptions.                      | `eyebrow`, `heading`, `subtext` (markdown), `alignmentHorizontal` (`start`/`center`), `features[]`                                                                                             |
| `page-sections/explainers/feature-slider` | Carousel of features, one slide at a time.                               | `slides[]`                                                                                                                                                                                     |
| `page-sections/explainers/feature-split`  | One feature explained beside an image.                                   | `eyebrow`, `heading`, `subtext` (markdown), `buttonSections[]`, `imageSource`, `imageAlt`, `imageAspectRatio` (`none`/`portrait`/`square`/`landscape`/`widescreen`), `imageRounded`, `reverse` |
| `page-sections/explainers/stats`          | Row of big numbers with labels.                                          | `eyebrow`, `heading`, `subtext` (markdown), `stats[]`, `dividers`                                                                                                                              |
| `page-sections/explainers/steps`          | Numbered how-it-works sequence with optional images.                     | `eyebrow`, `heading`, `subtext` (markdown), `items[]`, `orientation` (`horizontal`/`vertical`), `imageAspectRatio` (`landscape`/`portrait`/`square`/`widescreen`)                              |
| `page-sections/explainers/timeline`       | Dated timeline for a company story, funding history, or what comes next. | `eyebrow`, `heading`, `subtext` (markdown), `layout` (`vertical`/`horizontal`), `entries[]`                                                                                                    |

- `faq-section` item (`items[]`): `title`, `contentSections`.
- `feature-grid` item (`features[]`): `title`, `description`, `iconName`, `iconColor`, `iconBackground`.
- `feature-slider` item (`slides[]`): `eyebrow`, `title`, `description`, `imageSource`, `imageAlt`.
- **`feature-slider`**: No section-level heading — each slide carries its own text.
- `stats` item (`stats[]`): `number`, `prefix`, `suffix`, `label`, `sublabel`.
- `steps` item (`items[]`): `image`, `imageAlt`, `contentSections`.
- `timeline` item (`entries[]`): `year`, `date`, `title`, `body`.

### Proof

| `_component`                              | Use for                                               | Key content props                                                       |
| ----------------------------------------- | ----------------------------------------------------- | ----------------------------------------------------------------------- |
| `page-sections/proof/logo-cloud`          | Row of client logos, with an optional heading.        | `heading`, `logos[]`, `grayscale`, `scrolling`                          |
| `page-sections/proof/testimonial-section` | Customer quote with the author's name and photo.      | `text` (markdown), `authorName`, `authorDescription`, `authorImage`     |
| `page-sections/proof/testimonial-wall`    | Several customer quotes as cards in a masonry layout. | `eyebrow`, `heading`, `subtext` (markdown), `testimonials[]`, `columns` |

- `logo-cloud` item (`logos[]`): `image`, `alt`, `url`.
- `testimonial-wall` item (`testimonials[]`): `text`, `authorName`, `authorDescription`, `authorImage`.

<!-- generated:catalog:page-sections:end -->

## Building blocks

Composed **inside** page sections (in `contentSections`, `buttonSections`, `formBlocks`, `items`, `slides`, etc.), not placed directly in `pageSections`. Path prefix omitted in the tables below — prepend `building-blocks/`.

<!-- generated:catalog:building-blocks:start (npm run docs:catalog) -->

### Core elements — `building-blocks/core-elements/<slug>`

| `<slug>`          | Use for                                                          | Key content props                                                                                                                                                                                                                                                                                                   |
| ----------------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `alert`           | Status message: info, success, warning, danger, or a note.       | `variant` (`info`/`success`/`warning`/`danger`/`note`), `title`, `text` (markdown), `iconName`                                                                                                                                                                                                                      |
| `badge`           | Small pill label for statuses, tags, and announcements.          | `text`, `iconName`, `showDot`, `link`, `showArrow`, `variant` (`neutral`/`brand`/`info`/`success`/`warning`/`danger`/`plain`/`accent`), `size` (`sm`/`md`/`lg`), `alignmentHorizontal` (`none`/`start`/`center`/`end`)                                                                                              |
| `button`          | Clickable button for actions and links.                          | `text`, `link`, `iconName`, `iconColor` (`default`/`blue`/`green`/`yellow`/`orange`/`red`/`purple`/`pink`/`cyan`), `iconPosition` (`before`/`after`), `hideText`, `variant` (`primary`/`secondary`/`tertiary`/`ghost`/`text`), `size` (`sm`/`md`/`lg`)                                                              |
| `counter`         | Number that counts up to a target value.                         | `number`, `prefix`, `suffix`, `alignmentHorizontal` (`start`/`center`/`end`), `size` (`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`/`4xl`)                                                                                                                                                                                   |
| `definition-list` | List of terms and their definitions.                             | `items[]`, `alignmentHorizontal` (`start`/`center`/`end`)                                                                                                                                                                                                                                                           |
| `divider`         | Horizontal line that separates content.                          | `paddingVertical` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`)                                                                                                                                                                                                                                                     |
| `embed`           | Embed for maps, iframes, or raw HTML.                            | `html`, `aspectRatio` (`square`/`landscape`/`portrait`/`widescreen`)                                                                                                                                                                                                                                                |
| `heading`         | Heading from h1 to h6.                                           | `text`, `level` (`h1`/`h2`/`h3`/`h4`/`h5`/`h6`), `size` (`default`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`/`4xl`), `alignmentHorizontal` (`start`/`center`/`end`), `iconName`, `iconColor` (`default`/`blue`/`green`/`yellow`/`orange`/`red`/`purple`/`pink`/`cyan`), `iconPosition` (`before`/`after`)                |
| `icon`            | Icon from the icon library.                                      | `name`, `size` (`none`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`), `color` (`default`/`blue`/`green`/`yellow`/`orange`/`red`/`purple`/`pink`/`cyan`), `background`, `alignmentHorizontal` (`none`/`start`/`center`/`end`)                                                                                                     |
| `image`           | Image from a local file or a URL.                                | `source`, `alternateSource`, `alt`, `decorative`, `sizes`, `widths[]`, `width`, `height`, `rounded`, `aspectRatio` (`none`/`square`/`landscape`/`portrait`/`widescreen`/`horizontal-strip`), `positionVertical` (`top`/`center`/`bottom`), `positionHorizontal` (`left`/`center`/`right`), `background`, `priority` |
| `list`            | List with icons, bullets, or numbers.                            | `listType` (`icon`/`bullet`/`numbered`), `items[]`, `direction` (`horizontal`/`vertical`), `alignmentHorizontal` (`start`/`center`/`end`), `size` (`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`/`4xl`)                                                                                                                      |
| `pagination`      | Page links for content that spans multiple pages.                | `page`, `showArrows`                                                                                                                                                                                                                                                                                                |
| `rating`          | Star rating out of five, with half stars and an optional number. | `value`, `max`, `size` (`sm`/`md`), `color` (`default`/`blue`/`green`/`yellow`/`orange`/`red`/`purple`/`pink`/`cyan`), `showValue`, `alignmentHorizontal` (`none`/`start`/`center`/`end`)                                                                                                                           |
| `simple-text`     | Paragraph with optional bold, italic, and links.                 | `text`, `alignmentHorizontal` (`start`/`center`/`end`), `size` (`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`/`4xl`)                                                                                                                                                                                                         |
| `social-links`    | Row of social media icon links.                                  | `links[]`, `variant` (`ghost`/`solid`), `size` (`sm`/`md`), `alignmentHorizontal` (`none`/`start`/`center`/`end`)                                                                                                                                                                                                   |
| `spacer`          | Adds space between content.                                      | `size` (`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`)                                                                                                                                                                                                                                                                       |
| `table`           | Table with a caption, header row, and optional stripes.          | `caption`, `headerCells[]`, `rows[]`, `firstColumnHeader`, `striped`, `compact`                                                                                                                                                                                                                                     |
| `testimonial`     | Customer quote with an optional name, role, and photo.           | `text` (markdown), `authorName`, `authorDescription`, `authorImage`, `alignmentHorizontal` (`start`/`center`)                                                                                                                                                                                                       |
| `text`            | Markdown text with headings, lists, and links.                   | `text` (markdown), `alignmentHorizontal` (`start`/`center`/`end`), `size` (`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`/`4xl`)                                                                                                                                                                                              |
| `video`           | Video from YouTube, Vimeo, or a local file.                      | `type` (`youtube`/`vimeo`/`local-source`), `title`, `videoId`, `source`, `autoplay`, `loop`, `thumbnail`                                                                                                                                                                                                            |

- `definition-list` item (`items[]`): `title`, `text`.
- `list` item (`items[]`): `text`, `iconName`, `iconColor`, `showIcon`, `link`.
- `social-links` item (`links[]`): `icon`, `link`, `label`.

### Wrappers — `building-blocks/wrappers/<slug>`

Containers that hold other building blocks (their child items are themselves `_component` blocks).

| `<slug>`           | Use for                                                            | Key content props                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------ | ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `accordion`        | Expandable panels that show or hide content.                       | `label`, `items[]`, `openFirst`, `singleOpen`                                                                                                                                                                                                                                                                                                                                                                                                             |
| `bento-box`        | Grid where items can span more than one column or row.             | `label`, `columns` (`2`/`3`/`4`), `minRowHeight`, `items[]`, `gap` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`)                                                                                                                                                                                                                                                                                                                                          |
| `button-group`     | Arranges buttons in a row or column with even spacing.             | `label`, `buttonSections[]`, `direction` (`row`/`column`), `alignmentHorizontal` (`start`/`center`/`end`)                                                                                                                                                                                                                                                                                                                                                 |
| `card`             | Groups related content in a card.                                  | `label`, `contentSections[]`, `beforeContentSections[]`, `afterContentSections[]`, `maxContentWidth` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`), `showBeforeAfter`, `paddingHorizontal` (`xs`/`sm`/`md`/`lg`/`xl`/`2xl`), `paddingVertical` (`xs`/`sm`/`md`/`lg`/`xl`/`2xl`), `colorScheme` (`inherit`/`light`/`dark`), `lockColorScheme`, `backgroundColor` (`none`/`base`/`surface`/`accent`/`highlight`), `background`, `link`, `rounded`, `border` |
| `card-grid`        | Grid of cards with a cover image and freeform content.             | `label`, `layout` (`grid`/`masonry`), `columns`, `gap` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`), `aspectRatio` (`landscape`/`widescreen`/`square`/`portrait`/`none`), `items[]`                                                                                                                                                                                                                                                                      |
| `carousel`         | Sliding gallery of items.                                          | `label`, `slides[]`, `autoPlay`, `pauseOnHover`, `autoScroll`, `loop`, `showIndicators`, `indicatorStyle` (`dots`/`fraction`/`thumbnails`), `showArrows`, `alignmentHorizontal` (`start`/`center`/`end`), `slidesToScroll` (`auto`/`1`/`2`/`3`/`4`), `slideWidthPercent`, `minSlideWidth`, `gap` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`)                                                                                                            |
| `content-selector` | Tabs that switch between content panels.                           | `label`, `items[]`, `navigationPosition` (`start`/`top`)                                                                                                                                                                                                                                                                                                                                                                                                  |
| `grid`             | Arranges content in a grid that adapts to the screen size.         | `label`, `layout` (`start`/`center`), `minItemWidth`, `maxItemWidth`, `items[]`, `gap` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`)                                                                                                                                                                                                                                                                                                                      |
| `image-carousel`   | Image carousel with a thumbnail strip.                             | `label`, `images[]`, `showArrows`, `loop`, `aspectRatio` (`none`/`square`/`landscape`/`portrait`/`widescreen`/`horizontal-strip`), `sizes`, `widths[]`                                                                                                                                                                                                                                                                                                    |
| `masonry`          | Column layout for mixed-height items that keeps reading order.     | `label`, `columns`, `gap` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`), `items[]`                                                                                                                                                                                                                                                                                                                                                                        |
| `modal`            | Popup dialog opened by a button.                                   | `label`, `heading`, `triggerText`, `triggerVariant` (`primary`/`secondary`/`tertiary`/`ghost`/`text`), `triggerSize` (`sm`/`md`/`lg`), `triggerIconName`, `size` (`sm`/`md`/`lg`/`xl`), `contentSections[]`                                                                                                                                                                                                                                               |
| `split`            | Two columns of content side by side.                               | `label`, `firstColumnContentSections[]`, `secondColumnContentSections[]`, `distributionMode` (`quarter-three-quarters`/`third-two-thirds`/`half`/`two-thirds-third`/`three-quarters-quarter`/`fixed-flexible`/`flexible-fixed`), `fixedWidth`, `minSplitWidth`, `alignmentVertical` (`top`/`center`/`bottom`/`stretch`), `reverse`, `reverseOrderOnMobile`, `gap` (`none`/`xs`/`sm`/`md`/`lg`/`xl`/`2xl`/`3xl`)                                           |
| `steps`            | Numbered how-it-works sequence with optional images.               | `label`, `items[]`, `orientation` (`vertical`/`horizontal`), `imageAspectRatio` (`landscape`/`portrait`/`square`/`widescreen`)                                                                                                                                                                                                                                                                                                                            |
| `timeline`         | Dated timeline. Vertical is a continuous list. Horizontal scrolls. | `label`, `layout` (`vertical`/`horizontal`), `entries[]`                                                                                                                                                                                                                                                                                                                                                                                                  |
| `video-modal`      | Popup video that plays when opened and pauses when closed.         | `label`, `type` (`youtube`/`vimeo`/`local-source`), `title`, `videoId`, `source`, `triggerStyle` (`button`/`poster`), `posterImage`, `posterAlt`, `posterAspectRatio` (`widescreen`/`landscape`/`square`/`none`), `triggerText`, `triggerVariant` (`primary`/`secondary`/`tertiary`/`ghost`/`text`), `triggerSize` (`sm`/`md`/`lg`), `triggerIconName`, `size` (`sm`/`md`/`lg`/`xl`)                                                                      |

- `accordion` item (`items[]`): `title`, `contentSections`.
- `bento-box` item (`items[]`): `colSpan`, `rowSpan`, `contentSections`.
- `card-grid` item (`items[]`): `image`, `imageAlt`, `link`, `contentSections`, `aspectRatio`.
- `carousel` item (`slides[]`): `contentSections`.
- `content-selector` item (`items[]`): `title`, `subtext`, `iconName`, `iconColor`, `contentSections`, `navigationPosition`.
- `grid` item (`items[]`): `contentSections`.
- `masonry` item (`items[]`): `contentSections`.
- `modal` item (`contentSections[]`): `popoverId`, `size`, `label`, `labelledby`, `fullScreenMobile`.
- `steps` item (`items[]`): `image`, `imageAlt`, `contentSections`.
- `timeline` item (`entries[]`): `year`, `date`, `title`, `body`.

### Forms — `building-blocks/forms/<slug>`

Compose these inside a `form` (or a page section's `formBlocks[]`, as in `cta-form`).

| `<slug>`       | Use for                                             | Key content props                                                                                                                                                                                                                                          |
| -------------- | --------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `choice-group` | Checkboxes or radio buttons for choosing options.   | `title`, `name`, `required`, `options[]`, `orientation` (`vertical`/`horizontal`), `multiple`                                                                                                                                                              |
| `date`         | Form field for choosing a date.                     | `label`, `name`, `required`, `value`, `min`, `max`, `hint`                                                                                                                                                                                                 |
| `file-upload`  | Form field for uploading files.                     | `label`, `name`, `required`, `accept`, `multiple`, `hint`                                                                                                                                                                                                  |
| `form`         | Groups form fields into one submission.             | `action`, `formBlocks[]`                                                                                                                                                                                                                                   |
| `hidden`       | Hidden form field for extra data.                   | `name`, `value`                                                                                                                                                                                                                                            |
| `input`        | Single-line text field.                             | `label`, `name`, `type` (`text`/`email`/`password`/`tel`/`url`/`number`), `placeholder`, `required`, `value`, `hint`, `iconName`, `iconColor` (`default`/`blue`/`green`/`yellow`/`orange`/`red`/`purple`/`pink`/`cyan`), `iconPosition` (`before`/`after`) |
| `range`        | Slider for choosing a number in a range.            | `label`, `name`, `min`, `max`, `step`, `value`, `unit`, `required`, `showValue`, `hint`, `tickStyle` (`uniform`/`nearest`/`none`), `tickCount`                                                                                                             |
| `segments`     | Connected buttons for choosing one or more options. | `title`, `name`, `required`, `options[]`, `iconOnly`, `multiple`, `keepStateOnRefresh`                                                                                                                                                                     |
| `select`       | Dropdown for choosing one option.                   | `label`, `name`, `required`, `options[]`, `placeholder`, `value`, `hint`, `iconName`, `iconColor` (`default`/`blue`/`green`/`yellow`/`orange`/`red`/`purple`/`pink`/`cyan`)                                                                                |
| `submit`       | Button that submits a form.                         | `text`, `variant` (`primary`/`secondary`/`tertiary`/`ghost`/`text`), `size` (`sm`/`md`/`lg`), `iconName`, `iconColor` (`default`/`blue`/`green`/`yellow`/`orange`/`red`/`purple`/`pink`/`cyan`), `iconPosition` (`before`/`after`), `hideText`, `disabled` |
| `textarea`     | Form field for longer text.                         | `label`, `name`, `required`, `placeholder`, `value`, `hint`                                                                                                                                                                                                |
| `toggle`       | On/off switch.                                      | `label`, `name`, `checked`, `required`, `value`                                                                                                                                                                                                            |

- `choice-group` item (`options[]`): `value`, `label`, `checked`.
- `segments` item (`options[]`): `value`, `label`, `checked`, `icon`.
- `select` item (`options[]`): `value`, `label`, `selected`, `disabled`.

<!-- generated:catalog:building-blocks:end -->
