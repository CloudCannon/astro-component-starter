# Shared by every stackable building block via a second _inputs_from_glob entry.
spaceBefore:
  comment: Space above this block. Default follows the block type; a Spacer overrides it.
  type: select
  options:
    values:
      - id: default
        name: Default
      - id: none
        name: None
      - id: tight
        name: Tight
      - id: loose
        name: Loose
