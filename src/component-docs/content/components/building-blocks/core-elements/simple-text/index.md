---
title: Simple Text
overview: 'A paragraph (`<p>`) with optional bold, italic, and links. Set size and alignment. For headings and lists, use [Text](/component-docs/components/building-blocks/core-elements/text) instead.'
slots:
  - title: default
    description: The content inside the Simple Text component (used when text prop is empty).
examples:
  - slugs:
      - primary
  - slugs:
      - align
  - slugs:
      - sizes
---
