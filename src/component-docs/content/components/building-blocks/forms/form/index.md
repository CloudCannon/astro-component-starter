---
title: Form
overview: 'Groups fields and buttons into a form that can submit data to a URL. On a live site a `successMessage` stays hidden until the visitor comes back with `?success` in the URL, which is what the form endpoint should redirect to; the example below shows that state as it would appear after a submission.'
slots:
  - title: default
    description: The contents of the Form.
order: 1

examples:
  - slugs: [success-message]
---
