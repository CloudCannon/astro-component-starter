---
title: Submit
order: 3
overview: "A button that sends form data. Supports variants, sizes, and optional icons. It belongs in a [Form](/component-docs/components/building-blocks/forms/form/)'s blocks — the examples below are each a form — because a submit button outside a form has nothing to send. For a button that links to a page, see [Button](/component-docs/components/building-blocks/core-elements/button/). For several actions side by side, see [Button Group](/component-docs/components/building-blocks/wrappers/button-group/), which takes Buttons, not Submits."
examples:
  - slugs:
      - variants
  - slugs:
      - sizes
  - slugs:
      - icons
  - slugs:
      - icon-colors
---
