---
title: Scroll Deck
description: Full-height cards that pin and stack as the page scrolls.
overview: 'Cards sit in one tall track, each pinned a little lower than the last, so scrolling slides every card up over the one before it and the covered cards show as paper edges above. A rail of anchor links marks the card on top. Budget one screen of content per card; anything longer scrolls inside the card. Below 768px nothing pins, the rail is hidden, and the cards stack flat.'
examples:
  - slugs: [primary]
  - slugs: [two-cards-no-rail]
---
