tabs:
  type: array
  label: Code tabs
  comment: One tab per code sample, each with its own label, code, and language.
  options:
    structures: _structures.codeBlockTabs
wrap:
  type: switch
  label: Wrap long lines
  comment: Wrap long lines instead of scrolling horizontally. Enabled by default.
copyButton:
  type: switch
  label: Show copy button
  comment: Adds a button that copies the code.
lineNumbers:
  type: switch
  label: Show line numbers
  comment: Adds a non-selectable gutter that numbers each code line.
