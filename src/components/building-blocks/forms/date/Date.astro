---
import FormLabel from "@component-utils/FormLabel.astro";
import { generateFieldId } from "@component-utils/formField";
import { isNonEmptyString } from "@component-utils/isNonEmptyString";

const {
  label,
  name,
  required = false,
  value,
  min,
  max,
  hint,
  error,
  class: className,
  useDefaultEditableBinding: _useDefaultEditableBinding = false,
  _component,
  ...htmlAttributes
} = Astro.props;

const fieldId = generateFieldId("date", htmlAttributes.id);
const hintId = isNonEmptyString(hint) ? `${fieldId}-hint` : undefined;
const errorId = isNonEmptyString(error) ? `${fieldId}-error` : undefined;
const describedBy = [hintId, errorId].filter(Boolean).join(" ") || undefined;
---

<div
  class:list={["date", className]}
  {...htmlAttributes}
>
  <FormLabel
    for={fieldId}
    label={label}
    required={required}
    requiredClass="date-required"
  />
  {
    hintId && (
      <p
        class="date-hint"
        id={hintId}
      >
        {hint}
      </p>
    )
  }
  <input
    type="date"
    name={name}
    id={fieldId}
    required={required}
    aria-required={required}
    aria-invalid={errorId ? "true" : undefined}
    aria-describedby={describedBy}
    value={value}
    min={min}
    max={max}
    class="field"
  />
  {
    errorId && (
      <p
        class="date-error"
        id={errorId}
      >
        {error}
      </p>
    )
  }
</div>

<style is:global>
  @layer components {
    .date {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-xs);
      margin: 0;
      margin-bottom: var(--spacing-md);
      width: 100%;

      .date-required {
        color: var(--color-error);
        margin-inline-end: var(--spacing-em-xs);
      }

      .date-hint {
        color: var(--color-text-muted);
        font-size: var(--font-size-sm);
        margin: 0;
      }

      .date-error {
        color: var(--color-error);
        font-size: var(--font-size-sm);
        margin: 0;
      }

      > .field[aria-invalid="true"] {
        border-color: var(--color-error);
      }
    }
  }
</style>
