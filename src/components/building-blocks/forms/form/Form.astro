---
import { spaceBeforeAttr } from "@component-utils/spaceBefore.mjs";
import RenderBlock from "@component-utils/renderBlock.astro";
import Alert from "@core-elements/alert/Alert.astro";

type FormBlock = { _component?: string };

const {
  action,
  method = "post",
  honeypotName,
  successMessage,
  formBlocks = [],
  spaceBefore = "default",
  class: className,
  _component,
  useDefaultEditableBinding = false,
  "data-children-prop": childrenDataProp,
  ...htmlAttributes
} = Astro.props;

const effectiveChildrenProp = childrenDataProp ?? (useDefaultEditableBinding ? "formBlocks" : null);
const arrayDataAttributes = effectiveChildrenProp
  ? { "data-editable": "array", "data-prop": effectiveChildrenProp }
  : {};

const hasFormBlocks = formBlocks?.length > 0;
const hasSlotContent = Astro.slots.has("default");

if (!_component && !hasFormBlocks && !hasSlotContent) return;

// A slot-built form can't be inspected; pass `enctype` to override.
const hasFileUpload = (formBlocks as FormBlock[])?.some((block) =>
  String(block?._component ?? "").endsWith("/file-upload")
);
---

<form
  class:list={["form", "stackable", className]}
  action={action}
  method={method}
  enctype={hasFileUpload ? "multipart/form-data" : undefined}
  {...htmlAttributes}
>
  {successMessage && (
    <Alert
      variant="success"
      text={successMessage}
      class="form-success"
      role="status"
      tabindex="-1"
      hidden
    />
  )}
  <div
    class="form-fields"
    data-space-before={spaceBeforeAttr(spaceBefore)}
    {...arrayDataAttributes}
  >
    <slot>
      {formBlocks && (
        <RenderBlock
          contentSections={formBlocks}
          useDefaultEditableBinding={!!effectiveChildrenProp}
        />
      )}
    </slot>
  </div>
  {honeypotName && (
    <div class="form-honeypot">
      <label for={`${honeypotName}-field`}>Leave this field empty</label>
      <input
        type="text"
        id={`${honeypotName}-field`}
        name={honeypotName}
        tabindex="-1"
        autocomplete="off"
      />
    </div>
  )}
</form>

<script>
  import { onPageLoad } from "@component-utils/onPageLoad";
  import { setupAllForms } from "./setup";

  onPageLoad(() => setupAllForms());
</script>

<style is:global>
  @layer components {
    .form {
      width: 100%;

      > .form-fields {
        align-items: flex-start;
        display: flex;
        flex-direction: column;
        gap: var(--spacing-md);
      }

      /* Not `hidden`: `display: none` drops it from the tab order and accessibility tree. */
      > .form-honeypot {
        display: none;
      }

      > .form-success {
        margin: 0 0 var(--spacing-md);
      }
    }
  }
</style>
