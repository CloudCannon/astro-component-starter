---
import Button from "@core-elements/button/Button.astro";

const {
  text = "Submit",
  variant = "primary",
  size = "md",
  iconName,
  iconColor = "default",
  iconPosition = "before",
  hideText = false,
  disabled = false,
  class: className,
  useDefaultEditableBinding = false,
  "data-prop": customDataProp,
  _component,
  ...htmlAttributes
} = Astro.props;

const effectiveDataProp = customDataProp ?? (useDefaultEditableBinding ? "text" : null);
---

<Button
  element="button"
  type="submit"
  text={text}
  variant={variant}
  size={size}
  iconName={iconName}
  iconColor={iconColor}
  iconPosition={iconPosition}
  hideText={hideText}
  disabled={disabled}
  {...effectiveDataProp ? { "data-prop": effectiveDataProp } : {}}
  class:list={["submit", className]}
  {...htmlAttributes}
/>

<style is:global>
  @layer components {
    form .submit {
      margin-top: var(--spacing-md);
    }
  }
</style>
