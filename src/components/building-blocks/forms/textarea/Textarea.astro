---
import FormLabel from "@component-utils/FormLabel.astro";
import { generateFieldId } from "@component-utils/formField";
import { isNonEmptyString } from "@component-utils/isNonEmptyString";

const {
  label,
  name,
  placeholder,
  required = false,
  value,
  hint,
  error,
  class: className,
  useDefaultEditableBinding: _useDefaultEditableBinding = false,
  _component,
  ...htmlAttributes
} = Astro.props;

const fieldId = generateFieldId("textarea", htmlAttributes.id);
const hintId = isNonEmptyString(hint) ? `${fieldId}-hint` : undefined;
const errorId = isNonEmptyString(error) ? `${fieldId}-error` : undefined;
const describedBy = [hintId, errorId].filter(Boolean).join(" ") || undefined;
---

<div
  class:list={["textarea", className]}
  {...htmlAttributes}
>
  <FormLabel
    for={fieldId}
    label={label}
    required={required}
    requiredClass="textarea-required"
  />
  {
    hintId && (
      <p
        class="textarea-hint"
        id={hintId}
      >
        {hint}
      </p>
    )
  }
  <textarea
    name={name}
    id={fieldId}
    placeholder={placeholder}
    required={required}
    aria-required={required}
    aria-invalid={errorId ? "true" : undefined}
    aria-describedby={describedBy}
    class="field"
    >{value}</textarea
  >
  {
    errorId && (
      <p
        class="textarea-error"
        id={errorId}
      >
        {error}
      </p>
    )
  }
</div>

<style is:global>
  @layer components {
    .textarea {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-xs);
      margin: 0;
      margin-bottom: var(--spacing-md);
      width: 100%;

      .textarea-required {
        color: var(--color-error);
        margin-inline-end: var(--spacing-em-xs);
      }

      .textarea-hint {
        color: var(--color-text-muted);
        font-size: var(--font-size-sm);
        margin: 0;
      }

      .textarea-error {
        color: var(--color-error);
        font-size: var(--font-size-sm);
        margin: 0;
      }

      > .field[aria-invalid="true"] {
        border-color: var(--color-error);
      }
    }
  }
</style>
