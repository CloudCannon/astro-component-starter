---
import CustomSection from "@builders/custom-section/CustomSection.astro";
import Heading from "@core-elements/heading/Heading.astro";
import SimpleText from "@core-elements/simple-text/SimpleText.astro";
import TeamItem from "@page-sections/collections/team-grid/TeamItem.astro";
import CardGrid from "@wrappers/card-grid/CardGrid.astro";

type MemberProps = Record<string, unknown>;

const {
  eyebrow = "",
  heading = "",
  subtext = "",
  teamMembers = [],
  sectionLabel,
  maxContentWidth = "2xl",
  paddingHorizontal = "lg",
  paddingVertical = "4xl",
  colorScheme = "inherit",
  lockColorScheme = false,
  backgroundColor,
  background,
  useDefaultEditableBinding = false,
  _component,
  class: className,
  ...htmlAttributes
} = Astro.props;
---

<CustomSection
  class:list={["team-grid", className]}
  label={sectionLabel}
  maxContentWidth={maxContentWidth}
  paddingHorizontal={paddingHorizontal}
  paddingVertical={paddingVertical}
  colorScheme={colorScheme}
  lockColorScheme={lockColorScheme}
  backgroundColor={backgroundColor}
  background={background}
  useDefaultEditableBinding={useDefaultEditableBinding}
  {...htmlAttributes}
>
  <SimpleText
    alignmentHorizontal="center"
    class="eyebrow"
    data-prop="eyebrow"
    text={eyebrow}
    size="sm"
  />
  <Heading
    class="team-grid-heading"
    level="h2"
    size="lg"
    alignmentHorizontal="center"
    data-prop="heading"
  >
    {heading}
  </Heading>
  <SimpleText
    class="team-grid-subtext"
    size="md"
    alignmentHorizontal="center"
    data-prop="subtext"
    text={subtext}
  />
  <CardGrid
    class="team-grid-grid"
    columns={3}
    gap="md"
    aspectRatio="square"
    data-children-prop="teamMembers"
  >
    {
      teamMembers.map((member: MemberProps) => (
        <TeamItem
          data-id="page-sections/collections/team-grid/team-item"
          data-editable="array-item"
          {...member}
          class="team-grid-item"
        />
      ))
    }
  </CardGrid>
</CustomSection>

<style is:global>
  @layer page-sections {
    .team-grid-heading {
      margin-bottom: var(--spacing-sm);
    }

    .team-grid-subtext {
      margin-top: 0;
    }

    .team-grid-grid {
      margin-top: var(--spacing-xl);
    }
  }
</style>
