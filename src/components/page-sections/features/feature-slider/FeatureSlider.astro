---
import CustomSection from "@builders/custom-section/CustomSection.astro";
import FeatureSliderItem from "@features/feature-slider/FeatureSliderItem.astro";
import Carousel from "@wrappers/carousel/Carousel.astro";

const {
  colorScheme = "inherit",
  backgroundColor,
  paddingVertical = "4xl",
  class: className,
  slides = [],
  editable = false,
  _component,
  ...htmlAttributes
} = Astro.props;
---

<CustomSection
  class:list={["feature-slider", className]}
  maxContentWidth="xl"
  paddingHorizontal="lg"
  paddingVertical={paddingVertical}
  colorScheme={colorScheme}
  backgroundColor={backgroundColor}
  editable={false}
  {...htmlAttributes}
>
  <Carousel
    label="Feature slider"
    slideWidthPercent={100}
    minSlideWidth={0}
    autoPlay={false}
    autoScroll={false}
    editable={false}
    data-component="page-sections/features/feature-slider/feature-slider-slide"
    data-prop="slides"
    data-editable="array"
  >
    {
      slides.map((slide: any, index: number) => (
        <FeatureSliderItem
          aria-label={`Slide ${index + 1} of ${slides.length}`}
          {...slide}
          editable={false}
        />
      ))
    }
  </Carousel>
</CustomSection>
